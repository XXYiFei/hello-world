char* strcpy(char* src)
{
    //计算长度
    int len=0;
    while(src[len]!=0)
        len++;

    //申请内存空间
    char* dst=(char*)malloc(len+1);

    //复制字符串
    for(int i=0;i<len;i++)
        dst[i]=src[i];

    //字符串结束标记
    dst[len]=0;

    return dst;
}